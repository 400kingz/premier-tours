TEST_RECIPIENT=
GOOGLE_API_KEY=
EMAIL_PROVIDER=
EMAIL_FROM=
EMAIL_SMTP_USER=
EMAIL_SMTP_PASS=
GCP_PROJECT_ID=
GCP_LOCATION=
GCS_BUCKET=
VEO_MODEL=
VEO_CREDENTIALS_PATH=
FIRESTORE_PREFIX=
PUBSUB_TOPIC=
PUBSUB_SUBSCRIPTION=
CORS_ORIGINS=
OUTPUT_DIR=
UPLOAD_DIR=

# ── Render engine ─────────────────────────────────────────────────────────
# higgsfield (default, continuous frame-chained drone tour) | veo (fallback)
RENDER_ENGINE=

# ── Higgsfield DoP (primary engine) ───────────────────────────────────────
# Drop your key here to enable generation. provider: higgsfield | wavespeed
HIGGSFIELD_API_KEY=
HIGGSFIELD_PROVIDER=
# HIGGSFIELD_API_SECRET=          # only if your account uses key+secret
# HIGGSFIELD_BASE_URL=            # override the provider's default base URL
HIGGSFIELD_MODEL=
HIGGSFIELD_MODEL_TIER=
HIGGSFIELD_CLIP_SECONDS=
HIGGSFIELD_COST_PER_CLIP_CENTS=
# HIGGSFIELD_MOTION_ID=           # optional DoP camera-motion preset UUID
